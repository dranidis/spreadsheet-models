/**
 * Necessary declarations so that GAS library
 * exposes the function to the client.
 */
let getData = SheetLib.getData;
let appendRow = SheetLib.appendRow;
let appendRows = SheetLib.appendRows;
let updateRow = SheetLib.updateRow;
let createSheet = SheetLib.createSheet;
let fillWithUnderScore = SheetLib.fillWithUnderScore;
let getRows = SheetLib.getRows;
let testLib = SheetLib.testLib;
